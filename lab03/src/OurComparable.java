public interface OurComparable {
    /**
     * returns a negative number if I am less than o.
     * returns 0 if I am equal to o.
     * returns a positive number if I am greater than o.
     */

    public int compareTo(Object o);
}
